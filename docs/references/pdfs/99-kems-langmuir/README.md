# KEMS / Langmuir Phase-1 PDF staging

Acquired for the MinerU OCR campaign (t-509 / kems-fetch).

These PDFs are **not yet** assigned `REF-NNN` registry IDs in `references.yaml`.
Canonical working copies + full acquisition status live in:

`docs-private/research/kems-corpus/`

When a source is promoted into the registry, move/rename per:

`NN-<topic>/REF-NNN-<short>.pdf`

Do not treat this folder as the sole archive — hashes and paywall notes are in the private manifest.
