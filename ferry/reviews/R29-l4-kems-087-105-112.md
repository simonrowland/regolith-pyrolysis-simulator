# R29 — L4 near-ready kems-087 / 105 / 112

**Repo:** regolith-pyrolysis-simulator  
**Green tip:** `fbe3491b2` (`origin/work-v064-green`); L4 base `2e9e17c3d` (green + JANAF store regen / fidelity pins / interval fo2 typing)  
**Commit under review:** `b5774475083320210321535e9cc749d81885237c` on `origin/empirical/l4-near-ready-kems-087-105-112-2026-09-22`  
**Stack (base→tip):** single commit `b57744750` extracts: land Ichise 1989 Table 1/5 Fe-Ta and Fe-Nb mole fractions on per-heat experiments

**Files:**  
- `data/literature/extracts/kems-112-ichise-1989.yaml` (**only** file in `2e9e17c3d...b57744750`)

**Intent:** Close printed composition + cell-chamber pressure on Ichise 1989 Table 1 (Fe–Ta) / Table 5 (Fe–Nb) heats. Leave vacuum KEMS oxygen GAP (no invent). kems-087 / kems-105 PDFs missing this lane — no invent; ASK filed.

**Attack surface:** wrong Table 1/5 molar fractions; Fe complement invent/renormalise; Table 3 EPMA phase-boundary used as bulk charge; invented fO2 / vacuum-as-fO2; cell pressure wrong vs printed ~1×10⁻⁴ Pa; invented landings on 087/105; cross-scope extract edits; READY overclaim.

**Method:** Isolated worktree `/workspace/repos/wt/slot-r29` detached at `b57744750`. Static diff vs `2e9e17c3d`. PDF `/workspace/ferry-inbox/pdfs-l3-l5/kems-112-ichise-1989.pdf` via `pdftotext` + page renders (Table 1 p.846 / Table 5 p.848; apparatus p.844). Live `Migrator._migrate_extract` (write=False) tip↔base on all three sources; `collect_consumer_inputs` + `engine_point_requests` for composition/oxygen/pressure/T routes. Focused pytest + extract validator. Mode: **ran-tests**.

---

## Findings

No P0 / P1 / P2 / P3 on the named claim.

---

## Attack checklist

| Attack | Result |
| --- | --- |
| Wrong Table 1/5 molar fractions | **Fail (safe).** All 20 Table 1 `X_Ta` and 20 Table 5 `X_Nb` heat notes + `initial_composition` solute digits match PDF molar-fraction column (heats 9…56 Ta; 8…27 Nb). Fe complement = `1−X` exact on all 40 (`bad=NONE`). Live migrate: **40/49** `normalized_composition` via `normalized_initial_composition` / DERIVED (Fe complement stamp). |
| Table 3 EPMA as bulk charge | **Fail (safe).** Tip notes refuse Table 3 as bulk (`kems-112…yaml:19`). Table 3 retained only as `ichise_1989_table3_phase_boundaries` phase-boundary observation (`:7509+`), not as `fe-ta-xta-*` / `fe-nb-xnb-*` charge maps. No heat experiment locator `table: '3'`. |
| Invented fO2 / vacuum-as-fO2 | **Fail (safe).** Tip: **0/49** oxygen selected; `oxygen_condition:missing_evidence` on all probed rows with bench (47). **0** `fO2_*` in observation `point_conditions`; **0** experiments with `fO2_control`. Extract has no numeric fo2/pO2 fields (only prose “no fO2 invented”). Base also 0 oxygen — tip does not invent. |
| Wrong cell pressure | **Fail (safe).** PDF §2.1: cell chamber “about … **1×10⁻⁴ Pa**” (ion source 5×10⁻⁵). Tip heats carry `pressure_environment.total_pressure_Pa` point `0.0001` with `approximate: true`. Live: **47/49** `pressure_boundary` / `printed_run_pressure` / PRINTED (matches claim). Base was already 9/11 on series; tip copies onto per-heat charges. |
| Invented 087/105 landings | **Fail (safe).** `git diff 2e9e17c3d HEAD` = **only** `kems-112-ichise-1989.yaml` (087/105 bit-identical, 0 bytes). Live tip≡base: 087 **0/16** READY (comp `unsupported_print_form` string range; O₂+P missing); 105 **0/13** READY (`missing_bench` all 13 — no apparatus FK invent). ASK `/workspace/ferry-inbox/ASK-pdfs-l4.md` documents missing PDFs. |
| READY overclaim / cross-scope | **Fail (safe).** Tip READY **0/49** on 112 (oxygen open — honest); write-up 0/49 matches. Scope: single extract path. Derived store not regenerated. |

---

## Live READY (engine_point)

| Source | Obs | Comp selected | O₂ | P | T | READY | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| **kems-087** tip≡base | 16 | 0/16 | 0 | 0 | 16 | **0/16** | string range comps; vacuum O₂; no point P |
| **kems-105** tip≡base | 13 | 0 | 0 | 0 | 0 | **0/13** | `missing_bench` ×13 |
| **kems-112** base | 11 | 0/11 | 0 | 9/11 | 7/11 | **0/11** | series-linked only |
| **kems-112** tip | **49** | **40/49** | **0/49** | **47/49** | **45/49** | **0/49** | 40 heats Table 1+5; O₂ GAP |

### What looks sound

- Table 1/5 → `fe-ta-xta-*` / `fe-nb-xnb-*` with printed solute + Fe complement; activity T=1873 K and ~1e-4 Pa cell pressure copied onto charges.
- Series experiment kept for Figs / Table 4/6 / metallography / geometry — comps correctly remain open there (`unsupported_print_form` on non-heat rows).
- Vacuum KEMS oxygen left open; no buffer/pO2 invent.
- 087/105 untouched this tip; PDF-missing lane honored.

### Residual notes (not severity)

- Write-up claims T selected **46/49**; live probe counts **45/49** (2 `missing_bench` + 2 `temperature_K:missing_evidence` among the 47 with bench → 45). Comp **40**, P **47**, READY **0**, oxygen **0** match the write-up. Off-by-one on T is documentation only — no wrong extract digit.
- Composition route authority is **DERIVED** (`normalized_initial_composition`) because Fe=`1−X` is filled; solute X is the printed Table column. Expected under DERIVED-when-derived doctrine.
- Branch tip later advanced past this SHA on sibling work (l4b); this review pins **`b57744750` only**.

---

## Tests

Worktree `/workspace/repos/wt/slot-r29` @ `b57744750`, venv `/workspace/repos/regolith-pyrolysis-simulator/.venv`, `-o addopts=`:

- `tests/battery/test_waypoints.py` + `tests/battery/test_printed_fo2.py`: **68 passed**
- Live migrate tip↔base probes (comps, fo2 invent, cell P, 087/105 identity, READY): **passed / attacks failed**
- `tools/validate_literature_extracts.py` kems-112: **OK**

No fix patch. Do not push. Do not commit to mailbox.

---

## Verdict rationale

Claim holds under adversarial PDF digit check and live migrate: Ichise Table 1/5 mole fractions + cell P land on 40 heats; vacuum oxygen stays GAP; 087/105 unchanged with honest blockers; no cross-scope drift; READY remains 0 as claimed.

VERDICT: R29 | LAND | P0=0 P1=0 P2=0 P3=0 | ran-tests

READY: kems-087 0/16; kems-105 0/13; kems-112 0/49 (comps+P closed on 40 heats; O₂ open)
