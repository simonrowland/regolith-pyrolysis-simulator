# V6 — verify S11 non-determinism + S17 masked exit status

**Verifier:** adversarial re-check of  
`/workspace/ferry-inbox/sweeps/S11-non-determinism.md` (4 P1 + 6 P2) and  
`/workspace/ferry-inbox/sweeps/S17-masked-exit-status.md` (2 P1 + 2 P2 + 2 P3).  
**Code base:** `origin/review/r6-r8-fix` @ `1f8df6cbe` (worktree `/workspace/repos/wt/slot-07`, branch `verify-v6-s11-s17`)  
**Sweep tip cited:** `origin/work-v064-green` @ `fbe3491b2` — ancestor of this base (+14 commits). Cited sites still present; line nums shifted slightly for migrate queue dump (`:9023-9043` was `:8960-8980`) and janaf `write_staging` dump (`:2097-2103` was `:2010-2015`).  
**Post-sweep touch:** `a49cef012` retargeted bench interval refusal reason only — **float coercion paths unchanged**.  
**Calibration:** VERIFY-RULES / Batch Y — P0 only when a wrong physics/data/score number lands **today**; process/CI masks and regenerate-non-determinism ≤ P1. Null hypothesis = false positive.  
**Method:** static audit + minimal constructed probes (hash-seed, DEX float vs Decimal, collect/refresh/ioreg shell shapes). READ-ONLY on product; scratch under `/tmp/v6-verify`. No full migrate/score; no push.

## Constructed probes (base `1f8df6cbe`, 2026-09-22 ~21:09 EDT)

```
PYTHONHASHSEED∈{0,1,42,99,12345} set→dict key orders: 5 distinct
  (SiO2/MgO/FeO/Al2O3/CaO)

wt_pct_to_mole_fraction({SiO2:60,MgO:40}) vs swapped keys
  → component tuple order flips; to_plain(a)==to_plain(b) is False

DEX ratio 1.234567890123456789
  float path  0.09151497721269986
  Decimal ln  0.09151497721269989570648733341
  str() unequal for 1.1 / 2.5 / 1e6 as well

bench float(Decimal("0.123456789012345678901234567890"))
  → 0.12345678901234568  (Decimal grain truncated)

collect_recipe_db.sh /tmp/... no-such-host-s17.invalid
  → "(no runs…)", "0 study dirs", exit 0

enginepatch shape: git -C non-git-dir diff > out; echo recaptured
  → echo runs, shape_exit=0, out empty (0 bytes)

enginepatch.sh verify (no pipe): three MISSING → exit 1
  (hand-rolled rc works; do NOT trust `| tail` — masks status)

sh -c 'false | grep -c IOSurfaceRootUserClient' → stdout "0", rc=1
  epoch_grind sample_iosurface path would record status="ok", count=0

Live compilations stamp clocks:
  janaf/burcat/nasa-glenn/sgte-unary manifest generated_at: 2026-09-06/07…
  janaf tables extraction.date: '2026-09-06'
extracts-v2 files with components:: 17 (matches sweep)
```

## S11 — verdict table (4 claimed P1 + P2 spot-check)

| finding | exists? | live on base? | your sev | smallest fix | shared-root |
|---|---|---|---|---|---|
| **S11-P1a** `generators/bench.py:97,106-107,164+` `float(...)` on Decimal waypoints into consumer payloads | **yes** — engine_point / kems / vacuum_pyrolysis coerce composition, T, P, fO2, masses, orifice, clausing through `float` | **yes** on `bench_generate` / generator call (tests import `engine_point_requests`; `a49cef012` did not remove floats — 15× `float(` still) | **P1** | Emit via `_dec_str` / Decimal JSON (or string) matching migrate grain; never `float` for durable bench artifacts | **N1** float sinks |
| **S11-P1b** `scripts/bench_generate.py:66-72` `json.dumps(..., indent=2)` without `sort_keys=True`; `dict(Counter)` summary order | **yes** — payload / evidence / summary writers | **yes** on generate (same CLI tests exercise write path) | **P1** | `sort_keys=True` on all three dumps; build summary maps via `dict(sorted(...))` | **N2** JSON canonical |
| **S11-P1c** harvest / manifest clocks: `tools/harvest_janaf_compilation.py:369,421`; `build_janaf_compilation_manifest.py:159`; `harvest_burcat_compilation.py:164`; `harvest_nasa_glenn_compilation.py:184`; `harvest_sgte_unary_compilation.py:70` | **yes** — `datetime.now` / `date.today` embedded | **yes** — committed `data/literature/compilations/*/manifest.yaml` carry `generated_at`; JANAF table YAMLs carry `extraction.date: '2026-09-06'` | **P1** | Stamp from source bytes / git tree mtime / explicit CLI `--as-of`; or omit clock from committed sidecars | **N3** clock-in-artifact |
| **S11-P1d** `score.py:492-498` DEX `Decimal(str(math.log10(float(ratio))))` | **yes** — comment admits float path; `headline_rows` publishes `median_abs_dex: str(median)` into score-report | **yes** on score for any DEX-eligible residual (P_SAT / activity / …) | **P1** (not P0: ulp-scale vs Decimal ln; not wrong physics at engineering grain, but bytes/`str` medians can differ cross-platform) | `ratio.ln() / Decimal(10).ln()` (or `Decimal.log10` where available); keep Decimal end-to-end | **N1** float sinks |
| S11-P2 `migrate.dump_yaml` `sort_keys=False` + `to_plain` Mapping preserves `items()` order (`:589-593`, Mapping branch) | **yes** | latent enabler (merge producer fixed b-549; sink still unsorted) | **P2** held | Defensive `sort_keys=True` or sort mappings in `to_plain` | N4 |
| S11-P2 `_printed_map_payload` (`:2065`) no key sort | **yes** — hash-seed probe 5 orders | latent | **P2** held | `dict(sorted(wt.items()))` | N4 |
| S11-P2 `wt_pct_to_mole_fraction` + `Composition.__post_init__` no oxide-name canonicalize | **yes** — order flips; 17 live extracts-v2 with `components:` | latent (regen of same input map is stable) | **P2** held | Sort components by oxide name in Composition | N4 |
| S11-P2 `write_outputs` migration-queue entries/aliases unsorted (`:9023-9043`) | **yes** — append order only | latent | **P2** held | Sort by `(work_id, observation_id, why)` / `(observation_id, source)` | N4 |
| S11-P2 janaf `write_staging` observations extend-order (`:2097-2103`) | **yes** — not `sorted(..., key=observation_id)` | latent (callers use `sorted(glob)`) | **P2** held | Sort observations/sources before dump | N4 |
| S11-P2 usgs_b1259/b1452/b1544 observation dumps (`:2236-2241` / `:2357-2361` / `:1800-1804`) | **yes** — same pattern | latent | **P2** held | Same sort-by-id before dump | N4 |

## S17 — verdict table (all 6 sites)

| finding | exists? | live on base? | your sev | smallest fix | shared-root |
|---|---|---|---|---|---|
| **S17-P1a** `scripts/collect_recipe_db.sh:6,14-16` — `set -uo pipefail` **without** `-e`; `rsync \|\| scp \|\| echo` turns total transfer failure into exit 0 | **yes** — repro exit 0 / 0 studies | **yes** whenever nodes unreachable | **P1** | `set -euo pipefail`; `\|\| { echo … >&2; rc=1; }`; exit non-zero if zero studies when nodes requested | **E1** missing errexit + `\|\|` success |
| **S17-P1b** `patches/scripts/enginepatch.sh:11,148-161` `cmd_refresh` — no `-e`; no `[ -d "$d" ]`; failed `git diff` still `echo … recaptured` and returns 0 (empty patch file) | **yes** — shape repro exit 0 / empty out; `sulfliq`/`mace` are single-patch refresh targets | **yes** on refresh against missing/non-git checkout | **P1** | `set -euo pipefail`; `rev-parse --is-inside-work-tree`; refuse empty patch unless `--allow-empty`; propagate git status | **E1** |
| S17-P2 same script missing `errexit` in verify/status/apply — residual silent continuation between hand-rolled `rc` checks | **yes** — `git diff > "$live"` after `-d` with no status check; empty live + empty want → false MATCH | latent (MISSING path correctly sets `rc=1`; verify exit 1 confirmed) | **P2** held | Same `set -euo pipefail`; treat unexpected non-zero as verify failure | **E1** |
| S17-P3 DRIFT diag `comm \| head -8 \| sed` under pipefail (`:121-122`) | **yes** — pipeline present | live diag only | **P3** held — **reasoning nit:** on base `rc=1` is set **after** the `head` pipes (not before); without `-e`, SIGPIPE does not abort, so DRIFT still fails closed via `return $rc`. Conclusion correct; not a FP. | `head … \|\| true` or `set +o pipefail` around display | — |
| S17-P3 `epoch_grind.py:186,281-304` `sh -c` ioreg\|grep **without** pipefail; any int-parseable stdout → `status="ok"` | **yes** — probe `status=ok, count=0, returncode=1` | live when IOSurface sampling enabled on Darwin | **P3** held (diagnostic monitor, not score/ledger) | `bash -c 'set -o pipefail; …'`; gate on `returncode` / stderr | — |
| S17-P2 `pack_recipe_db_starter.sh:11-14` packs empty collection with exit 0 | **yes** — `[ -d ]` only; honest tar under `set -euo pipefail` | live follows bad collect (defense-in-depth) | **P2** held | Refuse when `cache.sqlite` count is 0 | follows **E1** primary |

## Shared roots

1. **N1 — float sinks in durable / scored paths** (S11-P1a, S11-P1d): keep Decimal (or stable decimal string) through bench payloads and DEX residuals.  
2. **N2 — canonical JSON** (S11-P1b): `sort_keys=True` + sorted Counter maps in `bench_generate`.  
3. **N3 — no wall-clock in committed compilations** (S11-P1c).  
4. **N4 — sort at YAML sinks / Composition** (S11 P2 cluster): one defensive sort policy covers dump_yaml / printed maps / queue / generator observation lists.  
5. **E1 — `set -euo pipefail` + no `\|\| echo` success** (S17-P1a/b + P2 residual): one shell discipline fix; pack empty-guard is secondary.

## Counts

| class | S11 claimed | S17 claimed | verifier on base `r6-r8-fix` |
|---|---|---|---|
| **P1 confirmed** | 4 | 2 | **6** |
| **P1 false positive** | — | — | **0** |
| **P2 held** | 6 | 2 | **8** |
| **P3 held** | 0 | 2 | **2** (1 reasoning nit, conclusion intact) |
| **P0** | 0 | 0 | **0** |
| **Under-rated (promoted)** | — | — | **0** |
| **Already fixed on base** | — | — | **0** |

No severity inflation. S11 four live P1s are real regenerate/score non-determinism sinks. S17 two live P1s are real success-masked failures (collect total-node miss; refresh empty patch). P2/P3 ratings held; none under-rated to hide a live score/ledger wrong number.

## TL;DR

- Confirmed **P1: 6** (S11: 4, S17: 2); **P0: 0**; **false positives: 0**.  
- All S11 P2 (6) and S17 P2/P3 (4) held; one S17-P3 reasoning nit (`rc=1` after, not before, `head` pipes) — still fail-closed.  
- Nothing fixed between sweep tip `fbe3491b2` and base `1f8df6cbe` for these sites.

READY: /workspace/ferry-inbox/verify/V6-s11-s17.md
