# S13 — conservation / accounting leaks

**Repo tip (audited):** `fbe3491b2f515212e61f65ac8a2e4f424b868b5d` (`origin/work-v064-green`).  
**Worktree:** `/workspace/repos/wt/slot-01` (detached @ `fbe3491b2`). READ-ONLY on product code; writes only to `/workspace/ferry-inbox/sweeps/`. No push. No product commits.  
**Scope:** mass / mol / element balances in the simulator ledger (`simulator/core*`, `simulator/accounting/`, wall deposition, condenser train) — paths that create or destroy mass, double-book, or silently clip.  
**Predicate:** a live or latent path invents mass/atoms, destroys mass/atoms without a paired account, double-books the same mass into two sinks, or silently clips a debit/credit so the ledger/report closes by forgiving the difference.  
**Method:** static audit of `simulator/accounting/{ledger,lots,completeness,yield_disposition}.py`, `simulator/mass_balance.py`, `simulator/wall_deposition.py`, `simulator/condensation.py`, and oxygen / stage-0 / flow-mass paths in `simulator/core.py`, cross-checked against conservation tests (`tests/test_mass_balance.py`, `tests/test_sso_r_r20_state.py`). Mode: **static-only** (no pytest run this sweep).  
**Doctrine vs live gate:** hour-snapshot / optimizer closure uses `PyrolysisSimulator._flow_mass_out_kg` over `FLOW_MASS_ACCOUNTS` + `reservoir.*` + pipe-segment wall accounts (`simulator/core.py:12478-12486`, `:13966-14018`). Transition-level atom/mass conservation is enforced by `LedgerTransition.validate_conservation` (`simulator/accounting/ledger.py:257-291`). Secondary helper `MassBalance.check` is **not** on that gate (only referenced from `simulator/mass_balance.py` + tests).

| site (file:line) | predicate match | constructed trigger | live? | severity | P0? | fix direction |
|---|---|---|---|---|---|---|
| `simulator/core.py:6155-6225` (`_apply_oxygen_reservoir_exchange`) | Melt `melt_intrinsic_fO2_log` is updated with full `dn_to_headspace` while the atom ledger only books `dn_ledger_to_headspace`. When headspace O₂ inventory is below the managed floor capacity, `dn_ledger_to_headspace → 0` but `|dn_to_headspace| > 0` → **oxidizing chemical potential invented without O atoms**. Direction labelled `managed_headspace_to_melt`. Changes evaporation / redox driving force (results). Fe-respeciation refuses to mint Fe₂O₃ from this path (`managed_floor_unbacked`), but fO₂ itself still moves. | `CONTROLLED_O2` atmosphere, overhead enabled, `melt_intrinsic_fO2_log ≈ -10`, **zero** `process.overhead_gas` O₂ → `_apply_oxygen_reservoir_exchange()`. Locked by `tests/test_sso_r_r20_state.py::test_managed_o2_floor_relaxes_reducing_melt_without_real_o2_inventory` (asserts after O₂ == before O₂ while fO₂ rises). | live | P0 | **yes** | Do not advance melt fO₂ beyond the ledger-backed `dn_ledger_to_headspace`. Managed floor must either (a) refuse / hold fO₂, or (b) credit a typed external O source account into `mass_in` before moving fO₂. Keep the `managed_floor_unbacked` refusal on Fe respeciation. |
| `simulator/core.py:6157-6163` (same function; clamp split) | Even with some headspace O₂, effective absorbable uses `C_h = max(head_o2_mol, effective_floor_mol)` while ledger absorbable uses real `head_o2_mol`. Clamp can leave `|dn_to_headspace| > |dn_ledger_to_headspace|` → partial unbacked fO₂ advance (`exchange_clamped=True`, direction may still be `headspace_to_melt`). | Finite but small overhead O₂ vs large reducing melt; exchange clamps. See adjacent test `test_headspace_to_melt_clamps_when_holdup_finite`. | live | P1 | no | Drive fO₂ only with the ledger-committed Δn; surface the unbacked remainder as a typed refusal/notice, not a silent state update. |
| `simulator/accounting/ledger.py:952-991` (`origin_unattributed_shortfall` on full withdrawal) | When a debit is “full withdrawal” within tolerance but `amount > available` origins, the gap is padded into `unresolved` origin atoms (`max(0, amount - available)`). Species conservation of the transition can still pass; **origin-layer atoms are invented** to close attribution. | Mixed-origin or slightly stale origin projection; debit ≈ account element total within `withdrawal_tolerance` but origin sum short. | live | P1 | no | Refuse (or require explicit external-origin authorization) instead of minting unresolved origin to cover a shortfall. Keep float dust only under atom tolerance without renaming it as physical unresolved inventory. |
| `simulator/accounting/ledger.py:2133-2166` (`_reconcile_origin_projection`) | `physical = max(0.0, physical_atoms)` then `scale = physical / tracked_total`. Negative species atom projection is floored before origin rescale; if `tracked_total ≤ 0` and `physical > 0`, entire physical mass is written as **new unresolved** origin. Origin tags are destroyed/created without a transition lot. | Any account whose species mol and origin projection drift (post-float dust, sign-carrying residue), then a transition that reconciles origins. | live | P1 | no | Reconcile only within tolerance; refuse or quarantine when `|physical - tracked|` exceeds atom tolerance instead of silent rescale / invent-unresolved. Do not floor signed physical atoms away before comparing. |
| `simulator/mass_balance.py:74-105` (`MassBalance.check`) | Helper mass-out omits wall deposits, `cation_sulfate_feed_kg`, and `inert_melt_components_kg`. Hardcodes `'stage0_mass_balance_delta': 0.0` (ignores `ProcessInventory.stage0_mass_balance_delta_kg`). Can report false closure or false gap vs atom-ledger gate. Not used by `_flow_mass_out_kg` / hour snapshot. | Call `MassBalance().check(...)` with inventory holding cation-sulfate feed and/or wall deposits booked only on `process.wall_deposit*`. | latent (helper / tests; live gate is atom ledger) | P2 | no | Align helper with `FLOW_MASS_ACCOUNTS` (include wall + sulfate + inert, or delete helper). Propagate real `stage0_mass_balance_delta_kg` or drop the field. |
| `simulator/mass_balance.py:65-68` + `:118-124` (`check` / `product_summary`) | Condensed stage totals and `volatiles_collected_kg` are summed independently. Same species in both maps is **double-booked** in helper outputs. No production writer to `volatiles_collected_kg` found under `simulator/` (only dataclass default + helper/tests) → currently latent. | Test path already does it: `tests/test_mass_balance.py::test_product_summary_sums_duplicate_volatile_species` expects H₂O stage 2 kg + volatiles 3 kg → 5 kg. | latent | P2 | no | Keep volatiles disjoint from stage `collected_kg`, or exclude volatiles already present in stage totals. Add an invariant test that production routing never writes both. |
| `simulator/core.py:11154-11164` (`_stage0_products_from_buckets`) | Stage-0 product rollup includes gas/salt/chloride/sulfide only — **not** `cation_sulfate_feed`. Leftover sulfate feed is invisible to `MassBalance` stage0_products (and sits on excluded staging accounts for the atom-ledger flow gate until consumed). Incomplete carbon cleanup leaves mass outside helper mass_out. | Feedstock with cation sulfates and insufficient carbon for full `_apply_stage0_cation_sulfate_carbon_reactions`. | latent (helper) / live-in-staging until reacted | P2 | no | Include residual sulfate (and inert) in any non-ledger mass check; ensure stage-0 end-state moves leftovers into a `FLOW_MASS_ACCOUNTS` terminal or fails closed. |
| `simulator/accounting/lots.py:61-66` (`allocate_pool_withdrawal`) | After overdraft check, `amount = min(max(0.0, amount), available)` silently clips within relative/absolute tolerance. Requested withdrawal dust is destroyed rather than refused or carried. | Withdrawal `available + ε` with `ε ≤ tolerance`. | live | P2 | no | Clip only below a documented float eps and record a typed dust note; above eps refuse. |
| `simulator/accounting/ledger.py:1297-1300` (`project_account_kg`) | Display-only clamp drops negative dust (`-tol < kg < 0`) from outward projection. Canonical signed balances unchanged (commented). Reports/UI can look “cleaner” than the ledger. | Normal account with subtraction round-off slightly negative within projection tolerance. | live (display) | P3 | no | Keep refuse-below-tolerance; optional explicit `dust_clamped_kg` in projection metadata so auditors see forgiven dust. |
| `simulator/condensation.py:53` (+ capture path using η) | `η_stage = clip(n_capture / n_available, 0, 1)` — when band flux × area × time exceeds available vapour, capture request is capped. Mass is not destroyed if melt/overhead is only debited for actual capture; still a silent physics clip of demanded capture. | High J_band / area / residence vs small `n_available`. | live | P3 | no | Keep the cap; emit a `capture_supply_limited` notice when clip binds (some wall paths already track `supply_limited`). |
| `simulator/wall_deposition.py:86-133` | Segment candidates `min(supply, rate)` then rescale if `sum > rate_kg_hr`. Redistribution only — no create/destroy if booked amount matches scaled candidates. Saturation refusal `continue` leaves mass on gas-train route (documented). | Multi-segment wall with Σ candidates > evaporative supply. | no-hit (safe redistribute) | — | no | Keep scale-to-supply; ensure ledger booking uses the same scaled dict (already implied by condensation resolve path). |
| `simulator/core.py:11008-11039` (`_add_stage0_balance_residue`) | Former balance-plug path now **refuses** (`ValueError` … “instead of a balance plug”) and returns `0.0`. No invent/destroy. | Stage-0 products that do not close vs feedstock + external inputs. | no-hit (fail-closed) | — | no | Keep refusal; do not reintroduce plugs. |
| `simulator/accounting/ledger.py:257-291` (`validate_conservation`) | Transition debit/credit mass and element atoms must match within tolerance or raise `UnbalancedTransitionError`. | Any unbalanced `LedgerTransition.apply`. | no-hit (enforced) | — | no | Keep hard refuse on transition apply. |

## Counts

- Sites scored: **11** (including 2 explicit no-hit / safe rows documented in-table)  
- Finding sites (exclude no-hit): **9**  
- Live: **6** · Latent: **3**  
- P0: **1** · P1: **3** · P2: **4** · P3: **2**  
- P0 count: **1**

## No-hit areas (audited; already fail-closed or N/A)

- **Atom-ledger flow gate** (`_flow_mass_out_kg` + hour snapshot `mass_balance_error_pct`) includes wall deposits, condensation train, overhead gas, oxygen cisterns, and `reservoir.*` — primary closure surface.  
- **`LedgerTransition.validate_conservation`** refuses unbalanced mass/atoms at apply time.  
- **Stage-0 balance residue** refuses plugs (`_add_stage0_balance_residue`).  
- **Wall geometry weight** refuses missing area as zero (`AccountingError` / prior S6 note).  
- **Fe redox respeciation** refuses to materialize Fe₂O₃ from managed-floor fO₂ without real O₂ (`managed_floor_unbacked`).  
- **Wall candidate scaling** when Σ > supply redistributes rather than invents.  
- **`FLOW_MASS_EXCLUDED_ACCOUNTS`** staging (`stage0_*_feed`, `stage0_foulant`) — residual mass here fails the flow gate loudly rather than silently vanishing (by design).  
- **Completeness `max(0, gross - min(gross, excluded))`** is algebraically non-negative (defensive); does not invent product mol beyond gross.

## Notes

- Severity P0 reserved for paths that change a **result** (yield, rate, composition, fO₂-driven evaporation) by inventing/destroying mass or its thermodynamic proxy without ledger backing. The managed oxygen floor is the only P0 found in scope.  
- `MassBalance` helper gaps are scored because the class still claims conservation checking, but they are not the optimizer/hour hard gate.

SWEEP: S13 | tip=fbe3491b2f515212e61f65ac8a2e4f424b868b5d | sites=9 | live=6 | latent=3 | P0=1 P1=3 P2=4 P3=2 | path=/workspace/ferry-inbox/sweeps/S13-conservation-leaks.md
