# S19 — duplicated physical constants

**Repo tip (audited):** `1f8df6cbe4cfbca526192b92e8adb2c1e1780dfc` (`origin/review/r6-r8-fix`, detached).  
**Green tip (comparison):** `origin/work-v064-green` @ `2e9e17c3d` (rules seed `fbe3491b2` is an ancestor; state truncation block identical on green).  
**Worktree:** `/workspace/repos/wt/slot-01`. READ-ONLY on product code; writes only to `/workspace/ferry-inbox/sweeps/`. No push.  
**Scope:** whole-repo product paths (`simulator/`, `engines/`, `scripts/`, `tools/`, `benchmarks/`) for **R, k_B, N_A, 273.15, 101325, 1e5, F, ln10** defined more than once with **different precision or value**, instead of `simulator/physical_constants.py`.  
**Predicate:** a second (or Nth) binding of one of those constants that (a) does not import the leaf, and (b) differs in precision/value from the leaf **or** from another live binding of the same symbol. Same-value local copies of exact leaf numbers are included as weaker (P3) consolidation debt.  
**Leaf authority (`simulator/physical_constants.py`):**  
`GAS_CONSTANT=8.31446261815324`, `FARADAY=96485.33212`, `AVOGADRO=6.02214076e23`, `BOLTZMANN=1.380649e-23`, `CELSIUS_TO_KELVIN_OFFSET=273.15`, `PA_PER_BAR=1e5`, `STANDARD_ATMOSPHERE_PA=101325.0`. **No `LN10` in the leaf** (lives ad hoc / in `battery.identity`).  
**Seed shape:** dual R/F in `simulator/state.py` (documented six-sig truncations that move pinned evaporation heads if swapped).  
**Batch Y rank rule:** P0 only when a wrong number reaches a result/score/ledger **today**. Relative R shift ~3e-7 and F shift ~2e-10 are not P0; dual-authority / wrong Celsius offset ≤ P1/P2.  
**Method:** static definition scan + import-graph of `state` vs `physical_constants` consumers; mode **static-only** (import smoke via `.venv` for value compare).

| site (file:line) | predicate match | constructed trigger | live? | severity | P0? | fix direction |
|---|---|---|---|---|---|---|
| `simulator/state.py:113-121` — `GAS_CONSTANT=8.31446`, `FARADAY=96485.3321` (comment admits six-sig truncations of the leaf; `BOLTZMANN` matches leaf exactly) | Second R/F authority: ΔR/R ≈ **3.15e-7**, ΔF/F ≈ **2.1e-10**. Comment: swapping to the leaf **moves pinned evaporation-flux heads** (`tests/test_capacity_coupling.py::test_default_off_preserves_hot_fe_redox_split_head_result`). Consumers of **state** R/F include `core`, `evaporation`, `overhead`, `equilibrium`, `equipment`, `thermal_budget`, `electrolysis`, `extraction`, lazy builtins. Consumers of **leaf** R include `capacity_coupling`, `vapour_rail/{activity,channels,melt_activity_resolver}`, `alphamelts`, `transport_regime`, `thermal_train`. | Any campaign hour that both evaporates (state R) and scores vapour-rail / capacity coupling (leaf R); or re-export leaf R through `state` without re-pinning. | **live** | P1 | no | Single authority: re-export leaf from `state` **after** regenerating pinned heads, **or** make leaf the only import path and delete state copies. Do not leave two public `GAS_CONSTANT`/`FARADAY` names. |
| `engines/builtin/electrolysis_step.py:108` + `:234-246` vs `:312-314` + `:585-789` | **Same module** imports leaf `FARADAY` for provenance `delta_gf`/`delta_g` stamping at import, then **shadows** with `from simulator.state import FARADAY, GAS_CONSTANT` inside `evaluate` for Nernst / Faraday-law extents. Two F values in one provider. | Construct `ElectrolysisStep` evaluate with MRE controls that both read stamped provenance ΔG and apply Faraday-law moles (`uncapped_moles_reduced ∝ 1/F`). | **live** | P1 | no | Drop the inner `state` import of `FARADAY`/`GAS_CONSTANT`; use the module-level leaf (or the reverse — but one only). |
| `simulator/chemistry/ellingham_graph.py:30-32` — `GAS_CONSTANT_J_PER_MOL_K=8.31446`, local `CELSIUS_TO_KELVIN_OFFSET=273.15`, `ELLINGHAM_STANDARD_PRESSURE_PA=100000.0` | Comment: “Match `simulator.state.GAS_CONSTANT` without importing state” — **copies the truncated R**, then also forks 273.15 / 1e5 instead of the leaf. | Ellingham P_eff / T paths in chemistry graph (used by electrolysis voltage provenance). | **live** | P2 | no | `from simulator.physical_constants import GAS_CONSTANT, CELSIUS_TO_KELVIN_OFFSET, PA_PER_BAR` (accept pin churn) **or** import state deliberately with a single shared alias. |
| `simulator/chemistry/organics_pyrolysis.py:52-53` — `GAS_CONSTANT_J_PER_MOL_K=8.31446`, `CELSIUS_TO_KELVIN=273.15` | Same truncated-R fork as state; local Celsius name. | Organics pyrolysis rate / T conversion using module constants. | **live** | P2 | no | Import leaf (or state) once; delete locals. |
| Near-CODATA **R=8.314462618** (9 sig, ≠ leaf 8.31446261815324 and ≠ state 8.31446) — product: `simulator/condensation.py:220`, `simulator/vapour_rail/nasa_cea.py:80`, `engines/builtin/foulant_disposition.py:18`, `simulator/pumping_cost.py:42`, `simulator/melt_backend/melt_envelope.py:90`, `simulator/melt_backend/imcc_sf04/gas.py:30` | Third R precision band (~1.8e-11 vs leaf; ~3e-7 vs state). Condensation + NASA-CEA sit on live vapour / transport paths while evaporation still uses state R. | Condensation Knudsen / Chapman–Enskog helpers using `GAS_CONSTANT_J_MOL_K`; CEA polynomial / gibbs_battery via `nasa_cea.R_J_PER_MOL_K`. | **live** | P2 | no | Replace with `from simulator.physical_constants import GAS_CONSTANT as …`. |
| Near-CODATA R tools/bench/scripts — `tools/motzfeldt.py:137`, `simulator/diagnostic_helpers/extract_reproduction.py:2443`, `benchmarks/buffer_reproduction.py:78`, `scripts/generate_t609_nio_reference.py:30` | Same 8.314462618 band off the leaf; not the primary campaign path. | Motzfeldt ratio thermo; extract form-correction; buffer / T609 reference scripts. | latent | P3 | no | Import leaf. |
| `simulator/melt_backend/sulfliq_matte.py:77-80` — `_R_J_PER_MOL_K=8.31468` | **Distinct value** (matches SulfLiq `Phase::R` in Phase.cc; ΔR/R ≈ **2.6e-5** vs leaf). Documented intentional engine fidelity — still a fourth R in-tree. | SulfLiq matte μ / fugacity bridge when SulfLiq is commissioned. | **live** (engine path) | P2 | no | Keep as **named** `SULFLIQ_PHASE_R_J_PER_MOL_K` next to a comment + test that pins Phase.cc; never alias as generic `GAS_CONSTANT`. Do not silently swap to CODATA inside the SulfLiq adapter. |
| `simulator/diagnostic_helpers/binary_pot_scoring.py:313` (`_row_temperature_K`) | Celsius→K uses **`+273.0`**, not `273.15` / leaf offset (**0.15 K low**). | Scoring row with `T_C` and no `T_K`. | latent | P2 | no | `+ CELSIUS_TO_KELVIN_OFFSET` from leaf. |
| `simulator/diagnostic_helpers/binary_pot_scoring.py:633` (`_gamma_field_temperature_K`) | Same **`+273.0`** on `gamma_<NNNN>C` field names. | Fields `gamma_1370C` / `1380C` / `1390C` (e.g. Kambayashi 1985 extract). | **live** | P2 | no | Same leaf offset. |
| Inline **full** leaf R (same value, not imported) — `simulator/fe_redox.py:451,566`; `simulator/chemistry/melt_activity.py:52` (`R_KJ_PER_MOL_K=8.31446261815324e-3`); `tools/compose_vapour_rail_carriers.py:44`; `benchmarks/vant_hoff.py:75`; `simulator/battery/waypoints.py:142` (`_R=Decimal("8.31446261815324")`) | Duplicated definition of the leaf number (fe_redox avoids importing `state` for MOLAR_MASS cycle reasons, but could import the leaf). | fe_redox Holzheid / Ban-ya γ; melt-activity RT; compose / van’t Hoff / waypoints. | **live** (fe_redox, melt_activity, waypoints) | P3 | no | `from simulator.physical_constants import GAS_CONSTANT` (Decimal(str(GAS_CONSTANT)) where needed). |
| Same-value **101325** re-binds — `simulator/diagnostic_helpers/extract_reproduction.py:83` (`STANDARD_ATMOSPHERE_PA`); `tools/normalize_extract_store.py:135` (`ATM_PA`); `simulator/thermal_train.py:68` (`OXYGEN_NORMAL_BOILING_PRESSURE_PA`); plus bare `/101325` in `simulator/condensation.py:1630` | Exact leaf atm, forked names / literals. | Extract NBP / normalize atm→Pa / O2 boiling anchor / Chapman–Enskog atm pressure. | **live** | P3 | no | Import `STANDARD_ATMOSPHERE_PA`; keep domain aliases as `= STANDARD_ATMOSPHERE_PA`. |
| Same-value **1e5 / 100000** re-binds — `engines/builtin/foulant_disposition.py:19`; `simulator/pumping_cost.py:43`; `simulator/liner_life.py:35`; `tools/normalize_extract_store.py:136`; `engines/builtin/vapor_pressure.py:355`; `simulator/equilibrium.py:42`; `simulator/chemistry/ellingham_graph.py:32`; `simulator/diagnostic_helpers/extract_reproduction.py:85`; `simulator/battery/migrate.py:5359` | Exact `PA_PER_BAR`, forked. | Any bar↔Pa path in those modules. | **live** | P3 | no | Import `PA_PER_BAR` (Ellingham standard pressure = `PA_PER_BAR`). |
| Same-value **273.15** named re-binds — `tools/normalize_extract_store.py:138` (`C_TO_K`); `simulator/chemistry/ellingham_graph.py:31`; `simulator/chemistry/organics_pyrolysis.py:53`; `simulator/melt_backend/imcc_sf04/adapter.py:88` (`_KELVIN_OFFSET`) | Exact leaf offset under local names (plus many bare `±273.15` literals in evaporation/alphamelts/etc. — literals not counted as separate sites). | Normalize C→K; Ellingham/organics/IMCC T conversion. | **live** | P3 | no | Import `CELSIUS_TO_KELVIN_OFFSET`; gradually replace bare literals. |
| **LN10** multi-defs (leaf has none) — `simulator/battery/identity.py:150`; `simulator/diagnostic_helpers/gibbs_battery.py:80`; `simulator/melt_backend/melt_envelope.py:91`; `benchmarks/buffer_reproduction.py:79`; `scripts/generate_t609_nio_reference.py:31`; `simulator/reference_data/robie_waldbaum_1968_usgs_b1259_loader.py:49` (`Decimal("2.302585092994046")`); `simulator/reference_data/hemingway_haas_robinson_1982_usgs_b1544_loader.py:55` | `ln(10)` bound in ≥7 places; float/`Decimal(str(math.log(10)))` agree here, but there is no single shared symbol. | Any log10 Kf / dex projection using module `LN10`. | **live** | P3 | no | Add `LN10 = math.log(10.0)` (and Decimal companion if needed) to `physical_constants` / `identity`; import everywhere. |
| `simulator/diagnostic_helpers/timeseries_validation_lake.py:52` — `AVOGADRO_PER_MOL=6.022_140_76e23` | Same value as leaf `AVOGADRO`, local name (condensation correctly aliases `AVOGADRO_MOL = AVOGADRO`). | Flux → molecule-count conversion in timeseries lake. | latent | P3 | no | `from simulator.physical_constants import AVOGADRO`. |

## Counts

- Sites: **15** (cluster rows count as one site each; binary_pot 273.0 kept as two rows → two sites)  
- Live: **12** · Latent: **3**  
- P0: **0** · P1: **2** · P2: **6** · P3: **7**  
- P0 count: **0** (Batch Y: no wrong ledger/score from these precision forks at present magnitudes)

## No-hit areas (audited; not counted)

- **`simulator/physical_constants.py`** — single CODATA/SI leaf; `BOLTZMANN` / `AVOGADRO` exact; `PA_PER_BAR` / `STANDARD_ATMOSPHERE_PA` / `CELSIUS_TO_KELVIN_OFFSET` exact by definition.  
- **`state.BOLTZMANN`** — same digits as leaf (re-export debt only; not different precision/value).  
- **Source-era R (intentional transcription, must not become CODATA):** `battery/generators/janaf.py` `JANAF_R_J_PER_MOL_K=8.31441`; `usgs_b1452` / `usgs_b1544` `8.3143`; `usgs_b1259` `B1259_R_CAL=1.98717` + printed joule companion `8.31469`; loaders `robie_waldbaum_…` / `hemingway_…` matching bulletins. Policy documented in `battery/identity.py`.  
- **k_B** — no divergent binding found (waypoints embeds the leaf string in a `Value.point_of` only).  
- **STEFAN_BOLTZMANN** — out of named scope; note only: `state`/`equipment` use `5.670374e-8` while leaf/`thermal_train` use `5.670374419e-8` (same dual-authority pattern).  
- **Modules already on the leaf for R:** `capacity_coupling`, `transport_regime`, `vapour_rail/{activity,channels,melt_activity_resolver}`, `alphamelts`, `thermal_train`, `battery/identity` (R from leaf; LN10 still local).  
- **Bare `±273.15` literals** in `evaporation` / engines — same value as leaf; consolidation debt, not different precision (not site-counted beyond the named re-binds above).

## Method notes

- Enumerated assignments matching R / F / N_A / k_B / 273.15 / 101325 / 1e5 / LN10; classified vs leaf digits.  
- Confirmed `state.GAS_CONSTANT != physical_constants.GAS_CONSTANT` and Faraday inequality via `.venv` import.  
- Mapped `from simulator.state import GAS_CONSTANT|FARADAY` vs `from simulator.physical_constants import GAS_CONSTANT|FARADAY` to show live dual graphs.  
- SulfLiq and JANAF/USGS era constants treated as intentional exceptions in no-hit / named-pin fix direction.

SWEEP: S19 | sites=15 | live=12 | P0=0 P1=2 P2=6 P3=7 | tip=1f8df6cbe4cfbca526192b92e8adb2c1e1780dfc | path=/workspace/ferry-inbox/sweeps/S19-duplicated-constants.md
