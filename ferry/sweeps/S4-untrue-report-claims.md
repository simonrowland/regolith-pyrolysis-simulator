# S4 — a report that says something untrue

**Repo tip:** `fbe3491b2` (work-v064-green / live paths); code also checked on `origin/review/janaf-batch-2026-09-22` (waypoints sites identical; `scripts/bench_readiness.py` alias fan-out **fixed** there via `row_source_ids = source_ids[:1]`).
**Scope:** `simulator/battery/`, `scripts/` (bench/battery), `tools/validate_literature_extracts.py`, `data/literature/build_index.py`.
**Predicate:** readiness/gap/report code emits a reason, list, count or status that does **not** follow from the evaluated state (static lists, defaults that read as findings, empty lists, missing vs present-but-unusable confusion, counts that double count, sources that silently vanish).
**Seed:** b-556 / b-560 / R2 — `ferry/reviews/R2-readiness-gap-reporting.md` (empty `MISSING_EVIDENCE`, false `fO2_log` present, weak control/thermal presence). R2-fix.patch **not** applied on tip.
**Live corpus check (2026-09-22):** 16 multi-`source_ids` works (+24 alias rows) still fan out on tip readiness; ≥3 works carry `orifice_area_m2` with no `clausing_factor` (geometric-only escape → static gap text). Constructed thermal/oxygen/charge/pressure triggers remain **latent** (no live `conditions.fO2_log` in works).

| site (file:line) | predicate match | trigger input | live? | severity |
|---|---|---|---|---|
| `simulator/battery/waypoints.py:167-169` + `thermal_path` present map `:870-874` (`_schedule_temperature_present` / `_located_present`) | `_result` filters `present` to `missing`; when every OR member is marked present yet no route fires, emits `WaypointAbsence(..., MISSING_EVIDENCE, ())` — empty list under a missing-evidence reason. Seed R2 P1 / b-556. | Hold with `hold_duration_s` POINT present + `temperature_K` typed-absent on schedule; `conditions.temperature_K` CATEGORICAL (located present, not POINT) → no route; gap `missing=()`. Surfaces via `collect_consumer_inputs` / engine_point as `temperature_K missing_evidence ()`. | latent | P1 |
| `simulator/battery/waypoints.py:993` (`oxygen_condition` gap path) | Marks `fO2_log` present from `experiment.conditions["fO2_log"]`, but no oxygen route reads conditions `fO2_log` (only observation point / control / sweep / buffer). Omits `fO2_log` from missing while inventing evidence. Seed R2 P1. | `fO2_control=None`, `conditions.fO2_log=POINT(-10)`, T/P resolve (e.g. kems factory). Gap `missing==("experiment.fO2_control",)` only; same run without conditions `fO2_log` lists both. | latent | P1 |
| `simulator/battery/waypoints.py:1178-1188` (`_effective_escape_gap`) | After a **GEOMETRIC_ONLY** route is selected, stamps static `MISSING_EVIDENCE` `("bench.geometry.clausing_factor", "bench.geometry.orifice_area_m2")` without evaluating presence. When selected inputs include printed `orifice_area_m2`, the gap claims that field is missing. | Bench geometry with `orifice_area_m2` POINT and no / unusable `clausing_factor` → selected `printed_area` (GEOMETRIC_ONLY); kems constraint gap names `orifice_area_m2` as missing. | **LIVE** (e.g. works `10.2355_isijinternational.32.1276`, `10.6028_nbs.ir.81-2279`, `10.3406_bulmi.1983.7673` — orifice area present, no clausing) | P1 |
| `scripts/bench_readiness.py:299-348` (`report` fan-out over `work.source_ids`) | Attribution loop writes the same experiment readiness under **every** citation alias; `source_count` / per-source rows inflate. Counts do not follow one-work→one-row evaluated identity. (R8; tip only — review branch uses `row_source_ids = source_ids[:1]`.) | Any of the 16 multi-`source_ids` works (e.g. `janaf-4th`/`nist-janaf-4th`, Costa/`kems-007`/`REF-016`). Tip readiness JSON grows **+24** alias rows vs first-registered attribution. | **LIVE** | P1 |
| `simulator/battery/waypoints.py:431-441` (`charge_moles_by_species` empty-results absence) | When mass + `initial_composition` are `_located_present` but no species route fires (e.g. `MOLE_FRACTION` with unknown molar masses), `charge_inputs` all True → `MISSING_EVIDENCE` with `missing=()`. Same empty-list class as thermal. | Sample `mass_kg` POINT + `initial_composition` MOLE_FRACTION over species with no `_molar_mass_kg_mol`; no printed wt% path. | latent | P1 |
| `simulator/battery/waypoints.py:988-990` (`control_present`) | Treats any located pO2 / any buffer `state.is_value` as present control, including non-route-usable forms (`_log_pressure` reject, categorical pO2, unpublished buffer name). Omits `experiment.fO2_control` from missing. Seed R2 P2. | `oxygen_partial_pressure_Pa` POINT `0` or categorical; or `buffer="NOT_A_REAL_BUFFER"` with T/P resolved. | latent | P2 |
| `simulator/battery/waypoints.py:870-874` (thermal presence ≠ route acceptance) | Weak present predicates omit present-but-unusable OR members from missing when the other member is truly absent (incomplete gap text). Seed R2 P2. | Incomplete schedule (duration only) + truly absent conditions T → names only `experiment.conditions.temperature_K`. Interval conditions T + no schedule → names only `experiment.thermal_schedule`. | latent | P2 |
| `simulator/battery/waypoints.py:605-620` (`pressure_boundary` present map) | Derived route requires `relevant_volume`, but `pressure_inputs` only tracks printed pressure / gas_load / pump speed. When load+speed present and volume absent, missing lists only printed pressure — volume **silently vanishes** from the gap. | Unknown run pressure; bench `gas_load_Pa_m3_s` + `pumping_speed_m3_s` present; no chamber/cell volume → selected None; missing omits `relevant_volume`. | latent | P2 |
| `simulator/battery/waypoints.py:1299-1305` (RPS `temperature_points_only` branch) | Static `MISSING_EVIDENCE` list `("…schedule.points", "…setpoints_and_holds")` without evaluating which schedule fields exist or failed. Present-but-unusable ramps / partial schedule misreported as those two absences. | Conditions POINT T only (`temperature_points_only` selected) while schedule has ramps present but unsupported for composition. | latent | P2 |
| `simulator/battery/waypoints.py:1125-1132` (`g2` present map) | Lists `experiment.conditions.evaporation_alpha` as missing whenever unset, even though the route defaults alpha to unity (`ASSUMPTION`). Default-as-finding; b-556 test currently blesses naming it. | Escape resolved, surface absent, alpha unset → missing includes alpha though route would assume 1 if surface appeared. | latent | P3 |
| `simulator/battery/waypoints.py` call sites passing `tuple(…_inputs)` **and** `present=…` into `_result` (e.g. `:586`, `:621`, `:678`, `:874`, `:1132`, `:1149`) | Positional `missing` tuple is dead weight — overwritten by `present`. Cosmetic / residual from b-556. Seed R2 P3. | Any gap path using `present=`. | latent | P3 |

## Counts

- Sites: 11
- Live: 2
- P0: 0 · P1: 5 · P2: 4 · P3: 2

## No-hit areas (in scope, audited)

- `tools/validate_literature_extracts.py` — fidelity / sample / review_status errors follow evaluated doc state; no readiness gap lists or inflated counts of this class.
- `data/literature/build_index.py` — access/pdf/sidecar status and observation-id marker counts are derived indexes; marker count is documented as non-parse approximate, not a readiness gap lie.
- `scripts/battery_score.py` / `simulator/battery/score.py` — score/refusal rendering; no OR-set gap text or alias fan-out matching the predicate.
- `scripts/battery_migrate.py`, `simulator/battery/pins.py`, `generators/`, `identity.py`, `waypoints.py` G3 present map (honest AND factors), `normalized_composition` unsupported-print branch (b-560: names only present-unusable — matches intent).
- `origin/review/janaf-batch-2026-09-22` `scripts/bench_readiness.py` — alias double-count **remediated**; waypoints S4 sites still present (R2-fix not landed).

SWEEP: S4 | sites=11 | live=2 | P0=0 P1=5 P2=4 P3=2 | no-hit areas: tools/validate_literature_extracts.py; data/literature/build_index.py; scripts/battery_score.py; simulator/battery/score.py; scripts/battery_migrate.py; simulator/battery/pins.py; generators/; identity.py; G3 present-map; normalized_composition unsupported-print (b-560 OK); review-branch alias fan-out fix
