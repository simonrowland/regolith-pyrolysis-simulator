# S14 — stamped per step / cell, dropped at run level

**Repo tip (audited):** `fbe3491b2f515212e61f65ac8a2e4f424b868b5d` (`origin/work-v064-green`, short `fbe3491b2`).
**Worktree:** `/workspace/repos/wt/slot-04` (detached @ `fbe3491b2f515212e61f65ac8a2e4f424b868b5d`). READ-ONLY on product code; writes only to `/workspace/ferry-inbox/sweeps/`. No push. No product commits.
**Scope:** per-step / per-cell diagnostics, notices, authority, warnings, or refusals stamped on the step (or cell) that never aggregate into the run document, published report, or optimizer objective / trust gate.
**Predicate:** a flag is attached at step/cell granularity, and the run-facing surfaces that publish yields, ledger masses, or optimizer scores carry **no** corresponding flag (status stays success / `ok` / `bridge`).
**Seed (CONFIRMED live):** engine commissioning notices stop on the step; a **1500 °C AlphaMELTS** hour publishes yields, ledger masses, and optimizer objectives with **no** flag. Certified T band is **[1073.15, 1700] K** (`engines/engine_commissioning.py` `PUBLISHED_TEMPERATURE_CERTIFIED_K`); 1500 °C = 1773.15 K is outside band → `commissioning_notice` + `authority=extrapolated` + `certified_band` while `status` stays `ok`.
**Method:** static audit of stamp sites (`_apply_engine_commissioning`, TE `_commissioning_notice_fields`), `_record_equilibrium_status`, `HourSnapshot` packing, `build_per_hour_summary`, run payload assembly, `run_executor` campaign-summary harvest, and optimizer objective / backend-status gates. Mode: **static-only**.
**Batch Y calibration:** P0 only when a wrong number reaches a result/score/ledger **today**. Unflagged extrapolated / warned values are a live **flag-contract** break → **P1** (not P0), matching the S1–S9 P0 regrade. Ferry confirmed this seed as a real live gap; severity stays P1 under that rule.

| site (file:line) | predicate match | constructed trigger | live? | severity | P0? | fix direction |
|---|---|---|---|---|---|---|
| `simulator/melt_backend/alphamelts.py:2159-2198` (`_apply_engine_commissioning`) → `simulator/core.py:8923-8937` (`_record_equilibrium_status`) → `HourSnapshot` / `_make_snapshot` (`simulator/state.py:1027+`, `core.py:13900-14141`) **seed** | Out-of-band AlphaMELTS returns `status=ok` with `diagnostics.{commissioning_notice,authority,certified_band}` and a `CommissioningNotice:…` warning. Core copies **diagnostics only** onto `sim._last_backend_diagnostics` (warnings never copied). `HourSnapshot` has **no** commissioning fields; `_make_snapshot` never packs `_last_backend_diagnostics`. Published hour yields / ledger / objectives therefore carry no flag. | AlphaMELTS melt hour at **T=1500 °C** (or SiO₂ outside [30, 80] wt%) with successful equilibration; inspect `result.diagnostics['commissioning_notice']` vs hour payload / product ledger / objective. | live | P1 | no | Pack commissioning keys onto `HourSnapshot` and forward into `build_per_hour_summary` + run metadata (or mark `backend_authoritative=false` / refuse ranking when any hour carries `authority=extrapolated`). |
| `simulator/melt_backend/thermoengine.py:40-55` + `:326` (`_commissioning_notice_fields`) **seed twin** | Same commissioning keys stamped on ThermoEngine equilibria; same `_record_equilibrium_status` → snapshot / report / optimizer drop path as AlphaMELTS. | ThermoEngine hour outside certified SiO₂/T band; same inspection as seed. | live | P1 | no | Share the AlphaMELTS rollup: one run-level commissioning aggregate for all MELTS-family backends. |
| `simulator/runner/__init__.py:2429-2663` (`build_per_hour_summary`) **seed publication choke** | Published per-hour schema (yields, O₂, metals, condensation, Knudsen, vapour notices, …) never reads `sim._last_backend_diagnostics` for `commissioning_notice` / `authority` / `certified_band`. VP helper only cherry-picks vapour keys (`:1809-1846`). Hour JSON is the operator-facing step report. | Any commissioned hour; `per_hour` rows show masses / yields with no commissioning keys. | live | P1 | no | Add `commissioning_notice` / `authority` / `certified_band` (or rolled `engine_flags[]`) to the pinned per-hour schema; regenerate fixtures. |
| `simulator/optimize/objective.py:3146-3155` + `simulator/run_executor.py:466-507` **seed consumer** | Optimizer / run aggregation treat `backend_status == "ok"` (from `_backend_status_history` of per-call **status** tokens only) as usable. Commissioning never changes status, so objectives publish from extrapolated hours with no flag. SC-109 comment admits `ok` ≠ certification — and still scores. | Optimizer evaluate of a 1500 °C AlphaMELTS campaign; objective returns numeric scores with no commissioning / extrapolated-authority mark. | live | P1 | no | Attach a first-class `engine_commissioning` / `authority=extrapolated` carrier (or refuse ranking) whenever any step’s diagnostics carry commissioning; do not equate `status=ok` with certified-band. |
| `simulator/runner/__init__.py:1800-1846` (`_vapor_pressure_source_report`) | Run payload mines `_last_backend_diagnostics` **only** for `vapor_pressure_backend_status*` / fallback keys. Commissioning / domain authority sitting in the same dict are discarded at the report boundary. | Commissioned AM/TE hour; vapour-pressure source report present, commissioning absent from run document. | live | P1 | no | Forward an allow-list that includes commissioning keys, or stop implying the diagnostics bag was consulted when only VP facets are copied. |
| `simulator/core.py:8560-8608` (`_attach_post_equilibrium_sulfsat`) + `_last_sulfur_saturation_result` | Sulfur-saturation `out_of_range` / `unavailable` warnings are appended to `result.warnings` and stored on `sim._last_sulfur_saturation_result`. Neither warnings nor the result are packed into `HourSnapshot`, `build_per_hour_summary`, or run metadata / optimizer. Comment claims “UI / telemetry pick it up” — those are not the run/report/optimizer. | Feedstock with Stage-0 S inventory; post-eq SulfSat warns; published hour/run still clean. | live | P1 | no | Mirror sulfsat into snapshot + per-hour + run_metadata; escalate when gate is `out_of_range` if sulfur masses affect yields. |
| `simulator/core.py:12637-12658` (`_capture_campaign_summary` rump_expectation) + `simulator/run_executor.py:315-358` | Campaign-end `rump_expectation.warning` is appended to `_rump_expectation_warnings` and embedded in `StepResult.campaign_summary`. `run_executor` only inspects `campaign_summary` for **C4/C6 refusal** diagnostics — rump warnings never become run status, report flags, or optimizer signals. | Campaign whose rump diagnostic emits a warning; final run payload / objectives unflagged. | live | P1 | no | Forward `rump_expectation` (and `_rump_expectation_warnings`) into run_metadata; escalate when warning binds to reported rump masses. |
| `simulator/diagnostic_helpers/binary_pot_battery.py:2195-2226` (`equilibrate_cell` harvest) + `simulator/diagnostic_helpers/binary_pot_scoring.py:1208-1209` / `:1282-1283` | Engine commissioning / authority on `EquilibriumResult` is not harvested onto the cell (only `imcc_notices`); scoring envelopes then hardcode `authority="bridge"` from `engine_reason` alone. Cell/report path publishes numeric activities with no commissioning flag. **Overlap S7** (transit drop); listed because the battery **score/report** is the run-level consumer for cells. | Battery cell at 1773.15 K on alphamelts; cell/score row lacks commissioning / shows `authority=bridge`. | live | P1 | no | Land R18-style harvest + score read of cell authority/notices (`review/s7-battery-flags`); do not recount as a separate P0 on this tip. |
| `simulator/core.py:2414-2418` / `:13002` (`_last_phase_context_diagnostic`) | Per-site phase-context records accumulate on the sim for the step and are cleared next hour; never copied to `HourSnapshot`, per-hour summary, run document, or optimizer. | Any tick that writes phase-context diagnostics; run report has no phase-context rollup. | live | P2 | no | Pack a compact phase-context summary onto the snapshot / run_metadata, or delete the store if it is not a published flag. |
| `simulator/core.py` `_last_char_{lance,feo_reduction,contamination}_diagnostic` (writers ~10560-10693) | Char-path diagnostics are step-stamped on the sim; runner `_c0_char_diagnostic` only consumes `_stage0_foulant_diagnostics` partition_carbon rows — these `_last_char_*` bags never aggregate to the run report / optimizer. | C0 char lance / FeO reduction / contamination events; run_metadata lacks those diagnostic bags. | live | P2 | no | Include `_last_char_*` in `_c0_char_diagnostic` / run_metadata, or stop stamping them as operator-facing notices. |
| `simulator/core.py` `_last_extraction_completeness_diagnostic` / `_last_overlap_evaporation_diagnostic` / `_last_freeze_gate_diagnostic` (writers ~12159-12440, `:3256`) | Step-local completeness / overlap / freeze-gate diagnostics never appear on `HourSnapshot` or run/optimizer surfaces. | Campaigns that exercise extraction completeness, overlap evaporation, or freeze-gate paths. | live | P2 | no | Snapshot + per-hour export, or demote to debug logging with no predict-and-flag claim. |
| `simulator/state.py:1191-1195` + `core.py:14137` (`silent_zero_diagnostic`) and `state.py:1185-1189` + `core.py:13434-13435` (`partial_melt_offgassing_diagnostic`) | Stamped onto the hour snapshot (so step history can see them) but **not** copied into `build_per_hour_summary` or elevated as a run/optimizer flag. Comments mark both diagnostic-only / non-gating — still meet “never aggregates to run/report/optimizer” as flags. | Tick that records silent-zero notes or partial-melt offgassing; per_hour_summary / objectives unflagged. | live | P3 | no | If they remain diagnostic-only, document that on the run schema; if they should warn operators, thread into per-hour + run_metadata. |

## Counts

- Sites scored: **12**
- Live: **12** · Latent: **0**
- P0: **0** · P1: **8** · P2: **3** · P3: **1**
- P0 count: **0** (Batch Y: seed is confirmed live flag-contract break → P1, not wrong-number P0)

## No-hit areas (audited; already aggregate or out of class)

- **Knudsen regime** — on snapshot and forwarded by `build_per_hour_summary` via `_knudsen_regime_observables`; hard refusal path also sets run status.
- **Condensation refusals / vapour-batch extrapolation notices** — threaded into per-hour (`condensation_refusals_by_species`, `extrapolation_notices_by_species`); vapour-only transit drops remain S7/S7b.
- **Wall-deposit sticking authority** — run_metadata + objective coating authority path.
- **Shuttle refusal history / C4–C6 campaign refusals** — `run_executor` promotes to run `status=refused`.
- **MRE uncertified yield / Ellingham ladder / fe_redox_split / redox_source_breakdown / o2_bubbler** — packed on snapshot and (where applicable) per-hour observables.
- **`degraded_path_engagement` / melt-redox gate floor-fallback engagement** — rolled into run payload.
- **Stage-0 foulant partition_carbon** — consumed by `_c0_char_diagnostic` into run_metadata.
- **Metal projection drift** — on snapshot; runner-strict remaps nonempty drift to failed status.
- **S7 intermediary drops** (VR bridge, shadow provider, battery harvest) — class S7; battery score hardcode listed above only as run-level consumer overlap at P1.

## Notes

- Seed temperature check: published certified band `temperature_K ∈ [1073.15, 1700]` → 1500 °C is out-of-band while MELTS still runs (`notice+run`). Confirmed live hour.
- `_record_equilibrium_status` never copies `result.warnings`, so the string `CommissioningNotice:…` channel dies even earlier than the structured diagnostics bag (which itself dies at snapshot/report).
- Batch Y: P0 reserved for inventing/altering a numeric result/score/ledger today. This class is “flag stamped, never rolled up” → seed and siblings score **P1 live**.

SWEEP: S14 | tip=fbe3491b2f515212e61f65ac8a2e4f424b868b5d | sites=12 | live=12 | latent=0 | P0=0 P1=8 P2=3 P3=1 | path=/workspace/ferry-inbox/sweeps/S14-per-step-flags-dropped.md
