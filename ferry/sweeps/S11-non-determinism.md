# S11 — non-determinism in generated artifacts

**Repo tip:** `fbe3491b2` (`origin/work-v064-green`).  
**Worktree:** `/workspace/repos/wt/slot-10` (detached @ `fbe3491b2`). READ-ONLY on product code; writes only to `/workspace/ferry-inbox/sweeps/`. Do not push.  
**Scope:** anything written to `data/` or compared in tests — `simulator/battery/migrate.py`, `data/literature/build_index.py`, `simulator/battery/generators/`, reports (`score` / `bench_generate` / migration-report), plus harvest writers that stamp `data/literature/compilations/`.  
**Predicate:** generated artifact bytes (or test-compared serialisations) can differ across runs with the same logical inputs because of **dict/set iteration order**, **unsorted globs**, **timestamps**, **float repr**, or **hash-order** (`PYTHONHASHSEED`).  
**Seed:** prior b-549 — `set(old) | set(new)` leaked hash-seed key order into `dump_yaml(..., sort_keys=False)` (`tests/battery/test_migrate.py` `test_merge_located_mapping_order_is_hash_seed_stable`).  
**Method:** static audit of writers + `PYTHONHASHSEED={0,1,42,99,…}` probes on `_printed_map_payload` / `to_plain` / Composition; mode **static-only** (plus hash-seed subprocess probes; no full migrate/score regen).

| site (file:line) | predicate match | trigger input | live? | severity |
|---|---|---|---|---|
| `simulator/battery/migrate.py:585-593` (`dump_yaml`) + `:572-573` (`to_plain` Mapping branch) | YAML writers use `sort_keys=False`; Mapping serialisation preserves `items()` order with **no** key sort. Any hash-ordered / set-built dict that reaches `dump_yaml` becomes run-dependent bytes under `data/literature/works/`, `extracts-v2/`, `observations-v2/`, generator staging. b-549 fixed one producer (`_merge_located_mapping`); the sink still does not defensively sort. | Reconstruct any composition/lab/report mapping via set/hash iteration, then `dump_yaml` / migrate write. Probe: `_printed_map_payload({k: Decimal(i) for i,k in enumerate({"SiO2","MgO","FeO","Al2O3","CaO"})})` yields **5 distinct key orders** across `PYTHONHASHSEED∈{0,1,42,99,12345}`. | latent (enabler; merge path fixed) | P2 |
| `simulator/battery/migrate.py:2061-2062` (`_printed_map_payload`) | Printed oxide maps written with input iteration order only — no `sorted(wt)`. Feeds sample `printed_composition` / observation YAML via `to_plain` → `dump_yaml`. | Same probe as above; also any oxide map rebuilt from a `set` of oxide keys before print. | latent | P2 |
| `simulator/battery/migrate.py:1977-1998` (`wt_pct_to_mole_fraction`) + `simulator/battery/records.py:358-367` (`Composition.__post_init__`) | Mole-fraction `components` keep **input key order**; Composition does **not** canonicalize by oxide name. Same chemistry → different `to_plain` / YAML / dedupe equality (`to_plain(a)==to_plain(b)` is False when only key order differs). Live store has ≥17 `extracts-v2` files with `components:`. | `wt_pct_to_mole_fraction({"SiO2":60,"MgO":40})` vs `{"MgO":40,"SiO2":60}` — component list order flips; identical amounts. | latent (same extract YAML regenerates stably; non-canonical form) | P2 |
| `simulator/battery/migrate.py:8960-8980` (`write_outputs` → `migration-queue.yaml`) | Queue `entries` / `dedupe_aliases` written in append order with **no** sort by `(work_id, observation_id, why)` / `(observation_id, source)`. Relies on sorted discover/walk order staying the only producer order. | Two migrate walks that enqueue the same logical set via different intermediate dict/set iteration (or future unsorted discover) → different `data/battery/migration-queue.yaml` bytes. | latent | P2 |
| `simulator/battery/generators/janaf.py:2010-2015` (`write_staging`) | Per-shard `observations` and `sources` dumped in document-extend order; **not** `sorted(..., key=observation_id)`. Callers today use `sorted(glob)`, so tip regen is stable; the writer itself is order-faithful to the iterable. | Pass `documents` in two permutations of the same tables → different `janaf-*.yaml` observation order under staging / promoted `observations-v2`. | latent | P2 |
| `simulator/battery/generators/usgs_b1259.py:2236-2241` (+ `usgs_b1452.py:2357-2361`, `usgs_b1544.py:1800-1804`) | Observation lists dumped in record-extend order without sorting by `observation_id` (summaries *do* sort counters). Same pattern as JANAF. | Permute input JSON record order (bypassing `sorted(directory.glob)` callers) → different observation YAML order. | latent | P2 |
| `simulator/battery/generators/bench.py:89-104` (+ `:160-189`, `:235-259`) | Consumer payloads coerce Decimal waypoints through **`float(...)`** (`composition_mol`, `temperature_C`, `pressure_bar`, `fO2_log`, KEMS/RPS fields). Binary float truncation / platform float differ from Decimal grain used elsewhere (`_dec_str`). | `engine_point_requests` / `kems_case` / `vacuum_pyrolysis_preset` on any ready bench input; write via `bench_generate`. | live (on generate) | P1 |
| `scripts/bench_generate.py:66-72` | `json.dumps(payload|provenance|summary, indent=2)` **without** `sort_keys=True`; summary uses `dict(counts)` / `dict(reasons)` from `Counter` (first-seen key order). Combined with bench `float` payloads → non-canonical / float-repr artifacts compared by tests that load JSON. | `python scripts/bench_generate.py --consumer engine_point --output <dir>` (or kems/rps) on the migrated store. | live (on generate) | P1 |
| `tools/harvest_janaf_compilation.py:421` + `:369`; `tools/build_janaf_compilation_manifest.py:159`; `tools/harvest_burcat_compilation.py:164`; `tools/harvest_nasa_glenn_compilation.py:184`; `tools/harvest_sgte_unary_compilation.py:70-71` | Manifest / sidecar writers stamp **`datetime.now` / `date.today`** into committed `data/literature/compilations/*/manifest.yaml` (live tips show `generated_at: '2026-09-06…'` / `'2026-09-07…'`). Re-harvest always rewrites the clock field. | Re-run any of the harvest / `build_janaf_compilation_manifest` tools into the compilation tree. | live | P1 |
| `simulator/battery/score.py:492-498` (`compute_metric` DEX branch) | Dex residual uses `Decimal(str(math.log10(float(ratio))))` — float intermediate. Value flows through `to_plain` → `dumps_residual_line` / score-report medians (`str(median)`). Float grain ≠ Decimal `ln` path (probe: ratio `1.234567890123456789` → float path `0.09151497721269986` vs Decimal ln path longer). Cross-platform float can diverge further. | Score any DEX-eligible residual (thermochemistry / vapour rails); write `data/battery/residuals.jsonl` or `score-report.md`. | live (on score) | P1 |

## Counts

- Sites: 10  
- Live: 4  
- P0: 0 · P1: 4 · P2: 6 · P3: 0  

## No-hit areas (in scope, audited)

- **`data/literature/build_index.py` writers:** globs for extracts/pdfs/claims/store paths are `sorted(...)`; set unions written via `sorted(set(...))`; `git_uncommitted_paths` returns `sorted(set(ids))`; `extract_mirror_parity` id lists sorted; `yaml.safe_dump(..., sort_keys=False)` but payload lists/dicts built from sorted walks. `datetime.now` at `:1264` is used only for **stale_inbox** age checks — **not** embedded in `SOURCE_STATUS.yaml` / `INDEX.yaml`. Byte-identity test `test_source_status_regenerates_byte_identically` is consistent with this.  
- **`migrate.write_outputs` works / observations:** `sorted(result.works)`, `sorted(set(exp_ids|bench_ids))`, `sorted(observations, key=observation_id)`, `sorted(set(sources))`, `dict(sorted(aliases))`; `discover_extracts` / `iter_observation_store_paths` sort globs. Unsorted `works_dir.glob` / `observations_v2.iterdir` are **delete-only**.  
- **`_merge_located_mapping` (`:4848-4854`):** explicitly avoids `set|set` hash order (b-549); covered by `test_merge_located_mapping_order_is_hash_seed_stable`.  
- **Generator summaries (JANAF/USGS):** counter/census maps written with `dict(sorted(...))`; input record/table discovery uses `sorted(glob)`.  
- **`simulator/battery/score.py` census / headline tables:** `refusal_census` / `notice_backlog` / rail×engine groups already `dict(sorted)` / `sorted(groups.items())`; residual JSONL lines use `sort_keys=True`.  
- **`scripts/domain_report.py`:** prints sorted species rows; not a `data/` writer.  
- **`scripts/battery_migrate.py`:** CLI wrapper only.  
- **Float path elsewhere in migrate:** `to_plain` routes `float`/`Decimal` through `_dec_str` (stable `format(..., "f")` with trim).  
- **USGS formation_elements:** element tuple is `sorted(counts)`; reaction term order follows formula parse order (hash-seed stable in probe).

SWEEP: S11 | sites=10 | live=4 | P0=0 P1=4 P2=6 P3=0 | no-hit areas: build_index sorted walks + no clock in INDEX/STATUS; migrate write_outputs sorted works/obs/sources/aliases; _merge_located_mapping b-549; generator summary sorted counters + sorted globs; score census/jsonl sort_keys; domain_report; battery_migrate CLI; migrate _dec_str floats; USGS formation_elements sorted
