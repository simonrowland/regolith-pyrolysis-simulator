flask>=3.0
flask-socketio>=5.3
pyyaml>=6.0
plotly>=5.18
numpy>=1.26,<2  # vaporock imports np.bool8 (removed in numpy 2.0); >=2 breaks the engine stack
scipy>=1.12
pandas
psutil
# PetThermoTools, Thermobar, PySulfSat and VapoRock are installed
# editable by install-engines.py; ThermoEngine requires its native
# build. Run that provisioning stage after base dependency sync.
# The base lock does not pin those clones or native artifacts.
# Pytest plugins required by [tool.pytest.ini_options] addopts in pyproject.toml
# (`-n auto` needs pytest-xdist; `--timeout=300` needs pytest-timeout). Without
# these, `pytest` aborts during argument parsing before any test runs.
pytest>=8.0
pytest-xdist>=3.8
pytest-timeout>=2.4
