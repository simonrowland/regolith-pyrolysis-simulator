# Phase 2 Codex Goal Prompt Handoff

Five staged prompts are ready for later batch dispatch: `costa-jacobson-2015-olivine-kems.md` targets SiO alpha `0.030 +/- 0.002` through `builtin-evaporation-flux`; `schaefer-fegley-2004-io-lava.md` targets tholeiite total vapor pressure `7.72 Pa` at `0.05` decades through `vaporock`; `sossi-fegley-2018-volatility.md` targets `gamma(KO0.5)` inside `[6.3e-5, 7.1e-4]` through `builtin-overhead-gas-equilibrium`; `sublimation-kinetics-2023-minerals.md` targets Na sublimation rate `1.38 g/h/g regolith` through `builtin-evaporation-flux` with a factor-3 provisional tolerance because the fixture lacks an explicit tolerance; `sargeant-2021-h2-reduction-lunar.md` targets Apollo 10084 O2 yield `0.0094 kg/kg initial` through `builtin-stage0-pretreatment`, but its fixture is under `_adjacent-isru/` and is still a stub with no source-backed tolerance. Suggested execution order: Schaefer first, Costa second, Sossi third, Sublimation fourth, Sargeant fifth. Schaefer and Costa are most directly mapped to existing provider surfaces; Sossi is likely to expose the gamma=1 activity-model limitation; Sublimation should wait until the Tier C sintering/effective-surface-area feature request is accepted or be run as an expected-red diagnostic; Sargeant should wait for a real H2-reduction yield surface or an explicit decision that `STAGE0_PRETREATMENT` owns that comparison. Tier C feature requests surfaced: add sintering-corrected effective surface area in the evaporation flux path to unblock Shaw 2023 plus Cooper 2007/Wilkerson 2023, add a non-ideal melt activity coefficient surface for Sossi-style gamma anchors, and add or route an H2-reduction yield provider distinct from generic Stage 0 cleanup if Sargeant is meant to go green. Surprises: top-5 ordering still matches the 2026-05-16 triage; `INDEX.md` appears stale for Sargeant because the fixture lives under `_adjacent-isru/`; two Tier A fixtures lack explicit tolerances, so their prompts make that gap visible instead of editing fixture data.
